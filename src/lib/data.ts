import type { Tournament } from '@/types';

// This mock data is no longer used for tournaments.
// It is replaced by a real-time Firebase Firestore implementation.
export const mockTournaments: Tournament[] = [];
